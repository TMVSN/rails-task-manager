<h1><%= @task.title %></h1>
<h2><%= @task.details %></h2>
<h2><%= @task.completed %></h2>

<%= link_to "Go back to tasks", tasks_path(@task) %>
<%= link_to "update", edit_task_path(@task) %>
<%= link_to "Delete", task_path(@task), method: :delete, data: {confirm: "Are you sure?"} %>